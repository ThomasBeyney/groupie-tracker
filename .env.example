# Variables d'environnement pour Groupie Tracker

# Port sur lequel le serveur écoute (fourni automatiquement par Scalingo)
PORT=8080

# Environnement de déploiement (development ou production)
# En production, les headers de sécurité sont renforcés
ENVIRONMENT=production

# URL de base de l'API Groupie Trackers
API_BASE_URL=https://groupietrackers.herokuapp.com/api

# User-Agent pour les requêtes Nominatim (obligatoire selon leur politique)
# Format recommandé: "NomApp/Version (email@contact.com)"
NOMINATIM_USER_AGENT=GroupieTracker/1.0 (votre-email@exemple.com)

# Content Security Policy personnalisée (optionnel)
# Si non définie, une CSP sécurisée est appliquée automatiquement selon l'environnement
# CSP_POLICY=default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline';
